import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pagina de linkV3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
